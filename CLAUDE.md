# buildup365 — 디자인 가이드

이 폴더는 web-02(katanamrp.com 재구성)의 디자인 DNA 를 그대로 물려받는다. 아래 토큰·디렉티브·안티패턴은 web-02 의 것이며,
buildup365 에서 달라진 점만 먼저 적는다.

## buildup365 에서 달라진 점
- **메인 컬러는 톤다운 네이비 `#293d69`** (hover `#213255`). web-02 의 형광 노랑 `#eeff38` 자리를 전부 대체한다: 헤더 CTA 버튼, 히어로 주 버튼, 마지막 CTA 슬래브(`Slab bg="navy"`).
  네이비 슬래브 위 텍스트는 `#e6ebf5`, 강조/아이브로우는 `#b6c4e2`, 버튼은 흰 필 1개 + 흰 아웃라인. 아래 원문의 "노랑" 은 모두 "네이비" 로 읽는다.
- 무드 전환 슬래브도 네이비 계열: 파스텔 퍼플 `#cdbdff` 자리를 연한 스틸 네이비 `#d9e0ee` (잉크 `#1f2f52`) 로, 링크/아이브로우는 `#3f5a94`, 리스트 아이콘은 `#93a5c9` 로 바꿨다. 다크그린 `#132f05` 은 유지한다.
- 푸터의 강조 텍스트(모토, 연락처 아이콘)는 `#b6c4e2`.
- 언어: 한글. 디스플레이 `Sora` / 본문 `Open Sans` 뒤에 `Noto Sans KR` 을 폴백으로 붙인다 (`word-break: keep-all`). 이것이 유일한 세 번째 서체 예외다.
- 슬래브 톤에 `green`(#132f05) 을 추가했다 — 사역 기록, 코칭 인용, 강의 현장처럼 "무게"가 필요한 장에만 쓴다.
- 사진은 buildup365.com 에 임베드된 원본(로고 · 프로필 · 사역 사진 6장)만 쓴다. oopy 의 Notion 이미지는 가져오지 않는다.
  **예외: /gallery/ 의 사역 앨범** — oopy '교육 사진' 갤러리의 카드(사진 · 제목 · 날짜) 전부를 `src/data/album.ts` + `public/images/album/`(900px JPEG) 로 옮겨 연도 필터와 함께 모두 보여준다 (2026-09 요청).
- 흰 표면 + 칩 그림자(`.ui-chip`)는 "떠 있는 객체" 한 개에만: 홈 프로필 카드, 하위 히어로의 media 카드(모집중 카드, 필요한 순간, 요청서 안내, 지도 카드).
- 네이비 필은 헤더 CTA 버튼 1개 + 히어로 주 버튼 1개 + 페이지당 네이비 슬래브 1개(ContactCta 또는 /ask/ 의 요청 방법)에만.
- 카카오 브랜드색은 버튼에 쓰지 않는다. 카카오 링크도 검정/아웃라인 버튼에 chat 아이콘으로.
  **예외: 플랫폼 로고 타일** — /sns/ 의 채널 카드·빠른 연락·문의 창구처럼 "어느 서비스인지"를 한눈에 알아야 하는 자리에서는 인스타그램(그라데이션)·유튜브(#ff0000)·페이스북(#1877f2)·카카오(#fae100) 공식 로고 형태를 브랜드 컬러 타일(`.brand--*`) 위에 쓴다. 버튼·링크·아이브로우에는 여전히 쓰지 않는다.
- **슬래브 이음새 규칙 (katanamrp.com 실측)**: 두 슬래브가 만나는 선에는 곡선이 한쪽에만 있다. 히어로 바닥은 직선이고 다음 슬래브 위가 96/24, 밝은 본문 슬래브는 카드처럼 4면(`all`), 연한 네이비 슬래브는 위 직선 + 아래 72/72(`bottom-soft`), 그 다음 슬래브 위는 직선(`none`). 마지막 네이비 CTA 는 위 슬래브가 평평하면 `all`, 위가 이미 둥글면 `bottom`.
- 가운데 정렬 섹션의 아이브로우는 회색 알약(`.eyebrow.pill`), 네이비 위에서는 `pill--light`.
- 상태 배지: 모집중 = 검정 필, 모집완료 = 검정 아웃라인, 교육완료 = 8% 잉크 — 색으로 상태를 구분하지 않는다.
- **글자 크기는 토큰만 쓴다.** `--t-small` 14 · `--t-body` 16 · `--t-lead` 18 · `--t-h4` 20 · `--t-card` 24 · `--t-h3` 32 · `--t-h2` 42 · `--t-h1` 47 · `--t-stat` 40(큰 숫자). 새 px 값 금지, 최소 14px. 굵기는 h1·h2만 700, 나머지 제목·버튼·라벨은 600, 본문 400. 보조 글자색은 `--c-ink-60`(80%) 이하로 흐리게 하지 않는다.
- 과정 세부내용(`/courses/{slug}/`)은 oopy 상세 페이지의 속성 표(일정·시간·방식·비용·강사·대상·마감·상태·인원)와 본문 장(과정 안내 → 특징 → 진행 → 참가비 → 참여요건 → 강사 → 신청)을 원본 순서 그대로 옮기되, 원본 문구는 한 항목도 빼지 않는다. 이 페이지는 일부러 다른 페이지와 다른 장치를 쓴다: 히어로는 절취선 티켓(`.ticket`, 날짜 블록 + 할인 스티커), 3시간 내용은 세로 타임라인 스텝, 추천 대상은 체크리스트, 특징은 살짝 기울인 흰 스티커, 진행은 180분 바, 참가비는 가격표, 신청은 3단계 + 계좌 복사 버튼, 종합신청서 전문은 `<details>`. 히어로가 화면 밖으로 나가면 하단 고정 신청 바가 뜬다. 교육과정 카드는 slug 가 있으면 카드 전체가 링크다.

## 구조 규칙 (유지보수)
- 교육과정 일정·상태의 단일 출처는 `src/data/courseDetails.ts` 다. `courses` 목록은 거기서 자동 생성되므로 따로 만들거나 고치지 않는다.
- 문구는 `src/data/` 의 페이지별 파일에 있고, 페이지는 `src/data/content.ts`(재수출)에서만 import 한다. content.ts 에 값을 직접 쓰지 않는다.
- 페이지 파일은 섹션 조립만 하고, 반복되는 덩어리는 `src/components/{teaching,course,sns}/` 로 뺀다. 스코프 CSS 는 마크업과 같은 파일에 둔다.
- 수정 후 `npm run check` 와 `npm run verify` 를 통과해야 한다. verify 는 외부 링크 허용 목록, 내부 앵커, 과정 데이터 필수 값, px font-size 를 검사한다.
- 사람용 문서는 `docs/`(EDITING · REQUESTS · CHECKLIST). 구조가 바뀌면 README 의 구조 표와 함께 갱신한다.

## Design Taste
*Source: https://katanamrp.com/features/inventory-management/ — generated by /taste (1 page at 1440×900)*

### Tokens
- **Grounds (all warm, R ≥ G > B)**: page `#f3f2f0`, slab `#f9f9f7`, card `#e8e6e0` with `0.67px #dcd9d1` border, hero `#d1cdc3`. Pure white `#ffffff` only on product-UI mockups, logo tiles and one testimonial section (9.4% of area on the reference).
- **Accent**: `#eeff38` acid yellow, black text — primary CTAs and the closing CTA slab only (4.7% area). Secondary buttons `rgba(0,0,0,0.9)` fill or 2px `rgba(0,0,0,0.9)` outline.
- **Section hues**: pastel purple `#cdbdff` (inner card `#f4f2fc`, heading `#092332`), dark green `#132f05` with `#d4e5ce` text, footer `#161616`. Hue is applied to whole sections, never to components.
- **Text**: headings `#000`, body `rgba(0,0,0,0.7)`, inline link `#7f57ff`, list/card icons `#b9a6ff`.
- **Type**: Diagramm (proprietary → use Sora) 700 for h1 47.2 / h2 42 / h3 32 / card 24 / lead 18, 600 for nav 15.4 and quotes 28, line-height 1.15. Open Sans 400 for body 16 and card copy 14, line-height 1.6. Heading ladder 18 → 24 → 32 → 42 (×1.33); h1 only ×1.12 above h2.
- **Spacing**: 8px base (8/16/24/32/40/48/64/96), 20px paragraph margin. Section slab padding 96 (hero 205/96, final CTA 160/0, footer 180/64). Card padding `40px 36px`. Container 1200, narrow copy 785, page gutter 68, grid gap 32.
- **Radius**: buttons 8, cards 16, logo tiles 18, testimonial 28, plan cards 32, section slabs `96px 24px` (TL 96 / TR 24 / BR 96 / BL 24), hero bottom `72px`. Nested radii always shrink (96 > 16 > 8).
- **Shadows**: none on cards, tiles, buttons. `0 4px 28px rgba(0,0,0,0.05)` only on floating chips inside product mockups; `8px 4px 44px rgba(0,0,0,0.15)` on the scrolled nav bar.
- **Motion**: `cubic-bezier(0.19, 1, 0.22, 1)` for every transition, 0.25s (0.5s for reveals). `:focus-visible` styles and `prefers-reduced-motion` respected.

### Directives
- When two sections meet, always give the lower slab a `96px 24px` corner radius and paint the upper slab's colour behind the corner cut-out (`Slab prev/next`) — never a full-width rule or empty band — because a curved edge reads as "new chapter" faster than a straight one and the lopsided corner is the brand signature.
- When choosing a background for anything that holds a product screenshot, always use a warm neutral and keep the mockup white — because a white app on white paper disappears; on `#f9f9f7` it is the brightest object on screen.
- When a call to action is needed, always use `#eeff38` for exactly one button per viewport and black 90% or a 2px outline for the rest — because full-chroma yellow on warm paper is the highest-attention object possible and it only works while it is rare.
- When a card or tile is needed, always use a hairline border and a one-step tonal fill (`#e8e6e0` on `#f9f9f7`) with no shadow — because shadows are reserved for floating product-UI chips so that "if it floats, it is the software".
- When product UI must be shown, always build it as HTML/CSS (table, chips, dropdowns, status cells) and let the chips overlap the photo block — because the mockup, not an illustration, is the page's only decorative element.
- When copy needs a heading, always set it in the display face at 42px for section openers (even inside a page with an h1) and centre it in a 785px column when the section is symmetric — because every chapter gets near-hero weight and the page reads as equal chapters.
- When a mood change is needed (integrations, testimonials), always recolour the whole slab (`#cdbdff`, `#132f05`) rather than colouring icons, borders or eyebrows — because hue at section scale keeps the accent rule intact.

### Anti-patterns
- Never use a cool or pure gray; every neutral is warm-tinted.
- Never put yellow on icons, links, borders, eyebrows or backgrounds other than the final CTA slab.
- Never add a box-shadow to a marketing card, button, tile or testimonial.
- Never separate sections with a horizontal rule or whitespace; the slab corners do the work.
- Never use a third font family or a weight below 400 / above 700.
- Never round a child more than its parent (button 8 inside card 16 inside slab 96).
- Never hotlink or copy the reference's photos, product screenshots or partner logos.
